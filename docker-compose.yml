services:
  api:
    build:
      context: ./api
    volumes:
      - ./api:/app
    ports:
      - "5000:5000"
    environment:
      - FLASK_APP=app.py
      - FLASK_ENV=development
      - FLASK_DEBUG=1
    command: flask run --host=0.0.0.0 --reload

  ui:
    build:
      context: ./ui
    volumes:
      - ./ui:/app
      - /app/node_modules
    working_dir: /app
    command: npm run dev
    ports:
      - "3000:3000"
    depends_on:
      - api
